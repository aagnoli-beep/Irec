# Review M0 — 2026-08-05

Prima esecuzione del ciclo di quality review (agenti: security, architecture,
code-quality, test, docs → tech-lead-synthesizer) sulla milestone M0.

## Verdetto del synthesizer: BLOCK

| Reviewer | Verdict | Critical/Blocker | High | Medium | Low/Info |
|---|---|---|---|---|---|
| security | APPROVE | 0 | 0 | 6 | 3 |
| architecture | APPROVE | 0 | 0 | 0 | 4 (S1–S4) |
| code-quality | APPROVE | 0 | 2 | 3 | 2 |
| test | BLOCK | 1 (B1) | 3 | 2 | 4 |
| docs | BLOCK | 1 | 2 | 5 | 2 |

**Driver del BLOCK:**
1. `openapi.yaml` fuori sync col codice: `servers: /v1` rende `/health` e `/ready`
   formalmente `/v1/health`–`/v1/ready`, ma il codice le serve a root (docs-BLOCKER ≡ arch-S1).
2. Zero test sulla **firma non valida** del call-token: chiave diversa con stesso kid,
   HS256 algorithm-confusion, `alg: none` (test-B1).

---

## Sub-piano R0 — Sblocco M0 ✅ ESEGUITO (2026-08-05, suite: 30 test verdi)

| # | Sev | Finding (fusi) | Fix |
|---|---|---|---|
| 1 | BLOCKER | Drift contratto su /health–/ready | Server override per-path in `openapi.yaml` (+ body e 503 di /ready documentati) |
| 2 | BLOCKER | Nessun test di firma (test-B1) | 3 test: chiave diversa stesso kid, HS256-confusion, `alg:none` → 401 |
| 3 | HIGH | `tenant_id` nei log mai presente: ContextVar settato da dependency sync → threadpool (security-M1 ≡ quality-M3) | `get_call_context` async (verify in thread via anyio), set del var nel contesto della request; test di propagazione + anti-bleed |
| 4 | HIGH | `jwks_url` senza vincolo https (MITM ⇒ token forgiabili) + fetch JWKS fallita → 500 anonimo (security-M3 ≡ quality-M2 ≡ test-H1) | Enforcement `https://` (eccezione localhost), errori httpx → `503 jwks_unavailable`; test |
| 5 | HIGH | 500 senza log dell'eccezione né `x-correlation-id` (quality-M1 ≡ test-H3) | Catch nel middleware: `logger.exception` + risposta `{error, code}` con header garantito; test |
| 6 | HIGH | Startup silenzioso senza `IREC_JWKS_URL` (security-M2) | Fail-fast se `IREC_ENV=production` senza JWKS; `/ready` → 503 se verifier assente |
| 7 | quick-win | Commento errato su `jwks_url` (docs-M5), docstring verifier che promette il check entitlement (docs-M4), type hints mancanti (quality-H2), timeout magic number (quality-H1), matrice auth incompleta (test-M1 parziale) | Correzioni puntuali contestuali |
| 8 | quick-win | README senza tabella env var (docs-H2) | Tabella env var + nota sul comportamento senza JWKS |

## Sub-piano R1 — Hardening (da eseguire dentro M1)

| # | Origine | Azione |
|---|---|---|
| 1 | security-M4 | Dockerfile: utente non privilegiato (`USER irec`) |
| 2 | security-M5 | Lockfile (uv/pip-compile) + `pip-audit` in CI + action pinnate a SHA |
| 3 | security-M6 | docker-compose: bind `127.0.0.1:5432`, password da `.env` |
| 4 | security-L1 | Validazione charset/lunghezza `x-correlation-id` |
| 5 | security-L3 | JWKS refresh-on-miss alla rotazione chiavi (rate-limitato) |
| 6 | docs-H3, docs-M4 | Docstring su handler pubblici e `get_call_context` (errori dichiarati) |
| 7 | docs-M6 | Nota "storico — superato da docs/ARCHITECTURE.md dove divergente" in testa a IREC-architecture-brief.md |
| 8 | docs-M7, arch-S3 | CI: validatore OpenAPI reale (rinominare lo step attuale); verifica contratto↔implementazione entro M5 |
| 9 | docs-M8 | `CHANGELOG.md` con sezione draft del contratto |
| 10 | security-L2, arch-S2, docs-INFO | Hardening contratto: 401 nel contratto, `IngestRequest.payload` tipizzato per kind, esempi request/response |
| 11 | test-M2 | Test `validation_error` 400 (prima delle rotte M5) |
| 12 | quality-INFO-2 | Valutare `create_app` factory pura (`uvicorn --factory`) |
| 13 | arch-S4 | Anti-replay `jti` quando arrivano le mutazioni (M5) |

## Processo (a regime)

A fine di ogni milestone (o blocco significativo di codice): lanciare i 5
reviewer in parallelo sul diff/branch → tech-lead-synthesizer → sub-piano R<n>
in questa cartella → eseguire gli item bloccanti subito, gli altri nella
milestone successiva.
