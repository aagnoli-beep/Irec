# Review M3 — 2026-08-05 (commit `1ac0279`)

Quarto giro del quality gate, sul diff della milestone M3 (ciclo di
sincronizzazione, run asincrone, selettore provider, check ruolo RLS).

## Verdetto del synthesizer: BLOCK

| Reviewer | Verdetto | Critical/Blocker | High | Medium | Info |
|---|---|---|---|---|---|
| security | REQUEST_CHANGES | 0 | 3 | 2 | — |
| architecture | REQUEST_CHANGES | 0 | 1 (guardrail 11) | — | 5 |
| code-quality | APPROVE | 0 | 2 | 6 | 6 |
| test | REQUEST_CHANGES | 0 | 6 | 4 | 1 |
| docs | BLOCK | 1 | 1 | 3 | 2 |

Due condizioni di BLOCK: contratto fuori sync (403 non dichiarato sul GET) e
guardrail 11 violato (i log delle run in background perdevano
`x-correlation-id` e tenant: il middleware resetta i contextvar prima dei
BackgroundTasks). Il finding più convergente — segnalato da **quattro
reviewer** con PoC eseguiti — era la race TOCTOU sull'Idempotency-Key: due
retry concorrenti producevano un 500 sul vincolo di unicità, esattamente nel
pattern retry che il brief prescrive a Mind. Nota positiva di processo: il
test-reviewer ha verificato la regola d'oro **per mutazione** (neutralizzato
il fix dei movimenti già allocati, i test di non-regressione falliscono come
devono).

## Sub-piano R6 — ✅ ESEGUITO (suite: 321 test verdi, coverage 96%)

| # | Origine (fusi) | Fix applicato |
|---|---|---|
| 1 | docs B1 + docs H1 | 403 dichiarato sul GET; `providers_not_configured` nel catalogo 503; `missing/invalid_idempotency_key` enumerate nel 400; codici anomalia elencati nello schema |
| 2 | arch H11 (guardrail) | `correlation_id` e tenant catturati nell'endpoint e re-impostati nel task (`services/sync_run.py`), con reset nel `finally` |
| 3 | security H1 ≡ quality H2 ≡ test H1 | Race idempotenza: query per chiave (indice), `IntegrityError` risolto rileggendo la run vincente → 202 stessa run, mai 500; test della race con lookup "cieco" |
| 4 | security H2 | Idempotency-Key oltre 128 caratteri → `400 invalid_idempotency_key` (test incluso); limite nel contratto |
| 5 | security H3 | Una sola run attiva per tenant: `409 run_in_progress` (documentato e testato) — elimina anche la collisione fra run sovrapposte |
| 6 | security M1 ≡ quality M4 | `environment` e `providers` come `Literal`: un typo fallisce allo startup invece di finire nel ramo dev con mock attivi e RLS spenta |
| 7 | quality H1 | Full-scan eliminati dal ciclo: stato del tenant caricato una volta per fase (posizioni aperte, pagamenti, clienti), `TenantRepository.find()` con criteri indicizzati, niente N+1 su `_aggiorna_posizioni` |
| 8 | quality M5 ≡ arch oss. | Ciclo di vita della run estratto in `services/sync_run.py`; RUNNING committato separatamente → osservabile dal poll (test H5, decisione esplicitata) |
| 9 | quality M6 | Lingua del contratto decisa ORA: envelope `/v1` in inglese (`result`, `error`), dominio in italiano dentro `result` |
| 10 | test H2 | Cancellazione GDPR a run in coda: il task termina senza effetti né risurrezioni (test) |
| 11 | test H3/H4/H6 | Testati: pagamento su fattura inesistente → anomalia; fattura duplicata intra-lotto → una sola; pagamento manuale pre-esistente → dedup senza run FAILED |
| 12 | test H5 + M | Transizioni e timestamp della run asseriti; shape del GET contro il contratto; 503 providers; `ErroreCollegamento` dalle porte di stato |
| 13 | docs M | README: le migrazioni usano l'utente admin, non `irec_app`; ROADMAP: chi innesca il ciclo (on-demand ora, scheduling con M8) |
| 14 | quality M2 (parziale) | "Oggi" della run in UTC (`sync_run.py`), non più `date.today()` locale |

## Sub-piano R7 — Rinviato per milestone

| Origine | Azione | Quando |
|---|---|---|
| security M2 ≡ arch | Reaper delle run stale (QUEUED/RUNNING dopo crash) + stale-check nel lookup idempotenza | prima delle run reali (M8) |
| quality M1 | Colonna `movimento_id` dedicata su `Pagamento` invece del parsing `startswith` (id con `:` romperebbero l'aggregazione) | M8 (contratti reali) |
| quality M2 (residuo) | Unica sorgente di "oggi" anche per i mock di runtime (oggi congelato allo startup) | M4 |
| quality M3 | `DemoScenari` come factory invece di subclass di `dict` | occasione utile |
| quality INFO | `ErroreCollegamento` catturato anche su `recupera_*` (irraggiungibile coi mock, reale in M8) | M8 |
| security nota | Quoting della password in `infra/initdb` se promosso oltre il dev | M8 |
| docs INFO | Esporre `lookback_giorni` dal contratto (oggi solo interno) | M5 |
| security H3 (residuo) | Cap giornaliero di run per tenant (oltre al 409 sulla run attiva) | M5 (con /usage) |
