# Review M6 — 2026-08-05 (commit `5874232`)

Settimo giro del quality gate, sulla proattività (brief + notifiche in polling).

## Verdetto del synthesizer: REQUEST_CHANGES

| Reviewer | Verdetto | Critical/Blocker | High | Medium | Low/Info |
|---|---|---|---|---|---|
| security | APPROVE | 0 | 0 | 1 | 1 |
| architecture | APPROVE | 0 | 0 | 0 | 3 |
| code-quality | REQUEST_CHANGES | 0 | 3 | 4 | — |
| test | BLOCK | 1 | 2 | 2 | — |
| docs | APPROVE | 0 | 0 | 3 | — |

Il giro più pulito sul piano di sicurezza e architettura: entrambi APPROVE,
con l'architecture-reviewer che ha confermato la **scelta del polling come la
più coerente col brief §4** (mantiene la dipendenza unidirezionale Mind→IREC;
un webhook la invertirebbe). Il BLOCK è arrivato dal test-reviewer per un gap
di copertura su una garanzia cardine, e un tema è emerso da **tre reviewer
insieme**: `POST /v1/notifications/ack` è una mutazione ma non richiedeva lo
scope `irec.write` (la costante era definita e mai usata).

## Sub-piano R9 — ✅ ESEGUITO (suite: 581 test verdi, coverage 96%)

| # | Origine (fusi) | Fix applicato |
|---|---|---|
| 1 | security M1 ≡ arch A ≡ quality M4 | `POST /v1/notifications/ack` ora richiede lo scope `irec.write` (`WriteContext`), come tutte le mutazioni M5; un token read-only riceve 403 |
| 2 | test B1 (BLOCKER) | Isolamento tenant su ack e notifications testato end-to-end: un id altrui nell'ack è no-op (marcate=0, notifica altrui intatta); `/notifications` non mostra le notifiche di un altro tenant |
| 3 | code-quality H1 ≡ arch C | `notifiche_da_consegnare`, `marca_lette`, `conteggio_per_tipo` usano `repo.find` con i predicati spinti al DB: l'indice `ix_notifica_tenant_letta` non è più morto |
| 4 | code-quality H2 | Predicato "non letta" estratto in `Notifica.viva()`, riusato dai find |
| 5 | code-quality H3 | `percentuale_recuperato`: collassato il doppio wrapper e cablato nel brief (campo `percentuale_recuperato`, esposto anche dall'API) |
| 6 | code-quality M3 | `emetti()` ritorna un `EsitoEmissione` esplicito (CREATA/RESUSCITATA/GIA_IN_CODA): il conteggio delle notifiche generate non include più le resuscitate |
| 7 | code-quality M1/M4 | Magic string `"domani passa a Recupero Crediti"` → costante; rimosso il dead code `COLLEGAMENTO_RISOLTO` e `SCOPE_WRITE` inutilizzato |
| 8 | test H1/H2/M1/M2 | Chiavi diverse coesistono; ack di una non tocca le altre; resuscita = una sola riga; confini T+43/T+45 nel ciclo; risoluzione del collegamento su due cicli consecutivi |
| 9 | docs M1/M2/M3 | ARCHITECTURE §4 registra la scelta polling (webhook punto aperto); docstring di `brief.py` riscritta (il tono lo mette l'LLM, non il dominio); `TipoNotifica` documenta quali tipi sono emessi in M6 e quali previsti (M8) |

## Sub-piano R10 — Rinviato per milestone

| Origine | Azione | Quando |
|---|---|---|
| code-quality M2 | `riferimento`/`dettaglio` del collegamento: campi separati invece di `stato:iso` da ri-parsare | quando servirà al consumatore |
| arch B | `Idempotency-Key` formale sull'ack (già idempotente per costruzione) | se il team Mind lo richiede |
| enum | Emissione effettiva di `ESCALATION_ESEGUITA` (notifica in-app) e `DATO_IN_RITARDO` (SLA AdE) | M8 (MS reali) |
| docs | Notifica "risposta debitore da gestire": parte del modulo AI conversazionale, fase 2 | fase 2 |
