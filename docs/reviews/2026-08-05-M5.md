# Review M5 — 2026-08-05 (commit `0ed4b25`)

Sesto giro del quality gate, sui ~19 endpoint `/v1` dei tool di Mind.

## Verdetto del synthesizer: BLOCK

| Reviewer | Verdetto | Critical/Blocker | High | Medium | Low/Info |
|---|---|---|---|---|---|
| security | APPROVE | 0 | 0 | 0 | 3 |
| architecture | REQUEST_CHANGES | 0 | 2 | 0 | 3 |
| code-quality | BLOCK | 1 | 3 | 4 | — |
| test | BLOCK | 2 | 4 | 2 | — |
| docs | BLOCK | 1 | 2 | 1 | — |

Security ha dato APPROVE pieno: l'impianto multi-tenant dei nuovi endpoint è
solido (id altrui → 404, scope enforced, input validati, niente PII). Il BLOCK
è nato da **quattro trigger indipendenti**: una regola di dominio ("scaduta")
finita in una rotta invece che nel servizio; il contratto che dichiarava
l'importo dell'aging come `number` mentre l'implementazione serializza stringhe
decimali; l'isolamento tenant testato su 1 rotta su 10; e un bug — pagamento
manuale su una fattura Insoluto → transizione di stato non gestita → 500.

## Sub-piano R8 — ✅ ESEGUITO (suite: 534 test verdi, coverage 96%)

| # | Origine (fusi) | Fix applicato |
|---|---|---|
| 1 | code-quality B1 ≡ arch H2 | Regola "scaduta" spostata nel dominio (`e_scaduta`) e nel servizio (`elenco_scadute`, filtro al DB); la rotta orchestra soltanto |
| 2 | docs B1 | `AgingReport.buckets[].importo` allineato a `string/decimal` come tutti gli altri importi |
| 3 | test B1 | Isolamento tenant parametrizzato su **tutte** le rotte con id di path (letture + azioni): id altrui → 404, nessuna mutazione della riga altrui |
| 4 | test H4 | Pagamento manuale su fattura Insoluto → `409 invoice_in_recovery` (era 500): è passata a Recupero Crediti, gli incassi si registrano da lì |
| 5 | arch H1 ≡ test B2 | `Idempotency-Key` del pagamento manuale spostata da body a **header** (coerente col resto del contratto); test cross-source end-to-end: salda a mano, poi il movimento bancario dello stesso incasso non viene ri-allocato |
| 6 | test H1 | Scope `irec.write` verificato su **tutte** le 8 mutazioni (parametrizzato), non solo su pause |
| 7 | test H2 | Confini esatti dei bucket di aging: -1/0/30/31/60/61/90/91 giorni |
| 8 | test H3 + M1 | KPI: portfolio vuoto, tutte insolute (`passato_a_recupero`), Premium che sblocca voice |
| 9 | code-quality H1 | `field_serializer` duplicato → tipo `ImportoStr` riusabile; rimosso `ImportoMixin` morto |
| 10 | code-quality H2 ≡ security low | `repo.list(Mandante)[0]` → helper `_mandante()` con `404 mandante_not_found` |
| 11 | code-quality H3 | `sostituisci_flusso` spezzata in `_valida_flusso` e `_riprogramma_comunicazioni` |
| 12 | code-quality M2/M3/M4 | `get_position` con `repo.get`; costante `STATI_CREDITO_APERTO` nel dominio (era duplicata in 4 punti); `calcola_consumo` filtra al DB |
| 13 | security low | Tetto sull'importo del pagamento manuale (`gt=0, le=1e8`): protegge dall'overflow NUMERIC |
| 14 | docs H2/H3/M4 | Contratto: `400` su `/invoices`, code completi in `BadRequest`/`Conflict`/`EntitlementError` (incl. `upgrade_required`, `invoice_in_recovery`) |
| 15 | docs (overstatement) | ARCHITECTURE precisa che la conformità automatica copre path+metodo, non lo shape |

## Sub-piano R9 — Rinviato per milestone

| Origine | Azione | Quando |
|---|---|---|
| code-quality M1 | Codici d'errore centralizzati in un `ErrorCode` (StrEnum) + test che ogni code usato sia nel contratto | M6 |
| arch minore | Estendere il conformance test allo *shape* (parametri, response, status) oltre a path+metodo | M6 |
| arch minore | Separare `AppError` da `errors.py` (che importa fastapi), così i servizi non tirano fastapi di transito | M6 |
| security low | Idempotency-key per-chiave vs per-fattura: valutare se legarla alla fattura | quando emergerà dall'uso |
