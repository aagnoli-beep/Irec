# Review M2 — 2026-08-05 (commit `65dd94c`)

Terzo giro del quality gate, sul diff della milestone M2 (porte dei
microservizi, mock a scenari, RLS Postgres, tooling R3).

## Verdetto del synthesizer: REQUEST_CHANGES

| Reviewer | Verdetto | Critical/Blocker | High | Medium | Low/Info |
|---|---|---|---|---|---|
| security | APPROVE | 0 | 0 | 2 | 4 |
| architecture | APPROVE | 0 | 0 | 3 rilievi | — |
| code-quality | APPROVE con commenti | 0 | 2 | 3 | 2 |
| test | REQUEST_CHANGES | 0 | 7 | 5 | 5 |
| docs | APPROVE | 0 | 1 | 4 | 2 |

Nessun CRITICAL né guardrail violato: la RLS è risultata **fail-closed e
senza injection** (verifica esplicita del security-reviewer). Il verdetto
nasce da 10 HIGH cumulativi concentrati su un tema: la quarta rete era
implementata bene ma **non verificata dove conta** — e da un tema convergente
segnalato da quattro reviewer indipendenti: la **migrazione RLS iterava i
modelli "vivi"** invece di una lista congelata, il che avrebbe rotto
l'upgrade su database vergine alla prima tabella aggiunta in M3.

## Sub-piano R4 — ✅ ESEGUITO (suite: 252 test verdi su SQLite e Postgres)

| # | Origine (fusi) | Fix applicato |
|---|---|---|
| 1 | security M-2 ≡ arch 1 ≡ quality H1 ≡ test H6 | Migrazione RLS con lista tabelle **congelata** alla revisione; nome policy esportato (`RLS_POLICY_NAME`) e downgrade generato dalla stessa fonte; test che percorre la catena alembic REALE su Postgres (upgrade → policy attive su ogni tabella → downgrade → policy rimosse) |
| 2 | test H5 | Test RLS estesi: UPDATE/DELETE deliberatamente senza filtro → 0 righe toccate; `pg_policies` copre **tutte** le tabelle dello schema con `FORCE` |
| 3 | test H7 | Test del guard sullo storico: riga di tenant-a "rubata" verso tenant-b da una sessione tenant-b → `TenantViolation` |
| 4 | test H1/H2/H3 | Semantica del riconciliatore mock decisa e pinnata: movimento a importo zero non è un incasso; fattura a importo zero **non svanisce** (invariante: ogni fattura in input è nell'esito); N movimenti → 1 fattura (completamento e parziale) |
| 5 | test H4 | Isolamento multi-tenant dei mock: stessa P.IVA su due tenant, nessuna contaminazione |
| 6 | security L-2 ≡ quality M1 ≡ test M5 | "Una sessione = un tenant": secondo repository con tenant diverso → `TenantViolation`; vincolo d'ordine repo/transazione documentato nel listener RLS |
| 7 | quality H2 | Alias `Any` motivati (`# HACK`: payload JSON esterni) |
| 8 | quality M2 ≡ docs M4 | Naming completato: `_set_rls_tenant`, `_block_cross_tenant_writes`, `_DELETION_ORDER`, `_not_ready_reason`; regola precisata in ARCHITECTURE (helper privati inclusi) |
| 9 | quality M3 | Ramo morto → `assert` che esplode invece di saltare il guard |
| 10 | security L-4 ≡ quality I2 | `assert` su invariante → `raise RuntimeError` (con `python -O` gli assert spariscono) |
| 11 | docs H1/M2/M3 ≡ quality I1 | ARCHITECTURE: "Quattro livelli" con attribuzione corretta M1/M2, bullet ricollocati; docstring mock "giorni di calendario"; README allineato ai gate CI (mypy, coverage) |
| 12 | docs I6, test M4/INFO | Porte: dichiarato `ErroreCollegamento` sulle porte di stato; assunzioni esplicitate (`(piva, numero)` univoca, importi in EUR); FIFO tie-break e duplicato con contenuto diverso pinnati; `log_event` testato |

## Sub-piano R5 — Rinviato per milestone

| Origine | Azione | Quando |
|---|---|---|
| security M-1 | Ruolo applicativo non-superuser nel compose + check startup/`ready` su superuser/BYPASSRLS (fail-fast in production) | M3 |
| security L-1 | Selettore mock/reale con fail-fast se production seleziona un mock | M3 (composizione provider) |
| arch 2 | Assegnazioni tipate mock→Protocol così mypy verifica le firme, non solo i nomi | M3 (al primo punto di iniezione) |
| arch 3 | Estendere il ban di layering a fastapi/httpx dentro `domain/` | M3 |
| test INFO | Split del mega-test RLS in 3 test | occasione utile |
| — | Regola di processo: ogni migrazione che crea una tabella applica la RLS a quella tabella (il test `test_policy_presente_su_ogni_tabella` la fa rispettare) | permanente |
